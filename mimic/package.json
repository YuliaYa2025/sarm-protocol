{
  "name": "sarm-mimic-task",
  "version": "1.0.0",
  "description": "Mimic Protocol task for SARM Protocol SSA rating updates",
  "type": "module",
  "scripts": {
    "compile": "mimic compile task.ts",
    "deploy": "tsx deploy.ts",
    "deploy:cli": "mimic deploy task.ts --key $MIMIC_DEPLOYMENT_KEY",
    "redeploy": "tsx redeploy.ts",
    "get-api-key": "tsx get-api-key.ts"
  },
  "dependencies": {
    "@mimicprotocol/lib-ts": "^0.0.1-rc.27",
    "@mimicprotocol/sdk": "^0.0.1-rc.23",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
